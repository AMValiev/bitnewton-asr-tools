# BitNewton ASR Tools

Инструмент для автоматической транскрибации (распознавания речи) и саммаризации (создания краткого содержания) аудиозаписей встреч.

---

## 🚀 Руководство пользователя

### 1. Установка

Проект поставляется в виде архива (ZIP или RAR).

**Для Windows:**
1. **Скачайте** архив с программой.
2. **Распакуйте** его в удобное место.
   - *Рекомендуется:* Распаковать в корень диска, например в `C:\BitNewton ASR Tools`.
   - *Важно:* Не запускайте программу прямо из архива! Обязательно извлеките папку.
3. Откройте распакованную папку.
4. Запустите файл `install-windows.ps1` (правой кнопкой мыши -> "Выполнить с помощью PowerShell").
5. Следуйте инструкциям на экране.
6. **Важно:** Если после установки окно PowerShell закрылось — просто откройте новое. Если вы устанавливали из уже открытого окна — перезапустите его или выполните команду `. $PROFILE`, чтобы обновить настройки.

**Для Linux/WSL:**
1. Распакуйте архив в домашнюю директорию: `~/BitNewton ASR Tools`.
2. Откройте терминал в этой папке.
3. Запустите: `./install-linux.sh`
4. Перезапустите терминал.

### 2. Настройка Токена

Перед первым использованием нужно сохранить ваш API токен.

```powershell
transcribe --set-token "ваш_токен_здесь"
```
Токен сохранится в файл `.asr_token` в вашей домашней директории:
- **Windows:** `C:\Users\ВашеИмя\.asr_token`
- **Linux:** `/home/user/.asr_token`

### 3. Как пользоваться

Откройте PowerShell (или терминал) в любой папке, где лежат ваши аудиофайлы.

**Транскрибация файла:**
```powershell
transcribe "Встреча.mp3"
```

**Автоматическое переименование (Телемост):**
Если вы скачиваете записи из Яндекс.Телемоста, они имеют длинные названия. Программа автоматически приведет их к удобному формату:
- *Было:* `Встреча в Телемосте 01.11.25 11-05-32 — запись Название.mp3`
- *Станет:* `202511011105 Название.mp3` (ГодМесяцДеньЧасМинуты)

Это упрощает сортировку и поиск файлов.

**Транскрибация + Саммаризация (Краткое содержание):**
```powershell
transcribe "Встреча.mp3" --summarize
```
*Что произойдет:*
1. Аудио отправится на сервер.
2. Скрипт дождется результата.
3. Создастся папка `Встреча/`.
4. В неё сохранятся: аудиофайл (переместится) и текст `Встреча_text.json`.
5. После распознавания текста, он отправится на саммаризацию.
6. Вы сможете выбрать промпт (шаблон) для анализа (например, "Детальный протокол").
7. Результат сохранится в `Встреча/Встреча_sum.md`.

**Только Саммаризация (если текст уже есть):**
```powershell
summarize "Встреча/Встреча_text.json"
```

### 4. Полезные команды

- `transcribe --help` — показать полную справку.
- `transcribe --list-prompts` — показать доступные шаблоны саммаризации.
- `transcribe file.mp3 --keep-original` — не перемещать исходный файл, а скопировать его.

### 5. Полный список аргументов

| Аргумент | Описание |
| :--- | :--- |
| `file` | Путь к аудиофайлу (обязательно). |
| `--summarize` | Включить автоматическую саммаризацию после транскрибации. |
| `--token TOKEN` | Использовать указанный токен (одноразово). |
| `--set-token TOKEN` | Сохранить токен в настройки и выйти. |
| `--output-dir DIR` | Указать папку для сохранения результатов (по умолчанию создается папка с именем файла). |
| `--keep-original` | Копировать исходный файл вместо перемещения. |
| `--prompt-id ID` | ID промпта для саммаризации (например, `meeting_detailed`). |
| `--model NAME` | Модель для саммаризации: `llama` (по умолчанию) или `gpt4`. |
| `--default` | Использовать настройки по умолчанию без лишних вопросов (полезно для автоматизации). |
| `--list-prompts` | Показать список всех доступных промптов (включая ваши кастомные). |

### 6. Работа с Промптами (Шаблонами)

Вы можете использовать готовые шаблоны для саммаризации или создавать свои.

**Выбор промпта:**
При запуске с флагом `--summarize` программа предложит выбрать шаблон из списка:
1. Встроенные шаблоны (от API).
2. Ваши личные шаблоны (из папки `prompts/`).
3. "Произвольный запрос" — возможность ввести инструкцию вручную прямо сейчас.

**Создание своего шаблона (Способ 1 - Автоматический):**
1. Запустите саммаризацию.
2. Выберите пункт **"Произвольный запрос"**.
3. Введите текст инструкции (например: *"Выпиши только список задач и ответственных"*).
4. После выполнения программа спросит: *Сохранить этот промпт для будущих запусков?*
5. Нажмите `y` и введите название (например, `Список задач`).
6. Готово! В следующий раз этот шаблон появится в списке.

**Создание своего шаблона (Способ 2 - Вручную):**
1. Зайдите в папку `prompts` в корне установленной программы.
   - *Если вы устанавливали по инструкции:* `C:\BitNewton ASR Tools\prompts`
2. Создайте текстовый файл, например `Мой шаблон.txt`.
3. Напишите внутри инструкцию для нейросети.
4. Сохраните. Он сразу станет доступен в программе.

---

## 🛠 Техническая документация

### Структура проекта

Проект организован модульно для удобства поддержки и распространения.

```
bitnewton-asr/
├── bin/                    # Исполняемые обертки (.bat)
│   ├── transcribe.bat      # Запускает src/transcribe.py через embedded python
│   └── summarize.bat       # Запускает src/summarize.py
├── src/                    # Исходный код Python
│   ├── transcribe.py       # Основной скрипт логики (CLI, файлы)
│   ├── summarize.py        # Скрипт саммаризации
│   ├── prompts_manager.py  # Менеджер кастомных промптов
│   ├── client.py           # Клиент API ASR (распознавание)
│   ├── summarizer.py       # Клиент API Summarization (LLM)
│   └── config.py           # Управление конфигурацией и токенами
├── prompts/                # Папка для пользовательских шаблонов (.txt)
├── python/                 # Embedded Python (портативная версия)
├── install-windows.ps1     # Скрипт установки для Windows
└── install-linux.sh        # Скрипт установки для Linux
```

### Принцип работы

1. **Запуск:** Пользователь вызывает команду `transcribe` в консоли.
2. **Обертка (Wrapper):** Скрипт `bin/transcribe.bat` перехватывает вызов. Он проверяет наличие встроенного Python (`python/python.exe`). Если он есть — использует его, иначе ищет системный Python.
3. **Изоляция:** Обертка устанавливает `PYTHONPATH` на папку `src/`, чтобы скрипты видели свои модули (`client`, `config`).
4. **Логика (src/transcribe.py):**
   - **Аргументы:** Разбирает параметры командной строки (`argparse`).
   - **Файлы:** Нормализует имя файла (исправляет даты из Телемоста), создает папку для результата.
   - **API:** Отправляет файл на сервер через `client.py` (polling статус задачи).
   - **Результат:** Скачивает JSON, сохраняет в папку.
   - **Саммаризация:** Если флаг `--summarize` активен, вызывает логику саммаризации, используя `summarizer.py`.

### Особенности реализации

- **Embedded Python:** Проект полностью автономен. В папке `python/` лежит минимальная версия Python 3.11, что позволяет запускать инструмент на машинах без установленного Python.
- **Интеграция в Shell:** Установочные скрипты не копируют файлы в системные папки, а создают `function` (PowerShell) или `alias` (Bash) в профиле пользователя, указывающие на текущее расположение проекта. Это позволяет легко обновлять ("git pull") или удалять проект (просто удалить папку).
- **Безопасность:** Токен хранится в домашней директории пользователя (`.asr_token`), а не в коде.

### Подготовка к разработке (Git)

В корне проекта находится `.gitignore`, исключающий:
- Временные файлы (`__pycache__`, `*.pyc`)
- Локальные настройки и токены (`.asr_token`, `.env`)
- Результаты работы (`*.json`, `*.md`, `*.log`)
- Папку `python/` (Embedded Python).

**Важно:** Репозиторий Git содержит только исходный код.
Для использования **полной портативной версии** (с включенным Python) скачивайте готовый архив из раздела **Releases**.
Если вы клонируете репозиторий вручную, вам потребуется самостоятельно скачать Embedded Python (см. `python/README_SETUP.md`) или использовать системный Python.
