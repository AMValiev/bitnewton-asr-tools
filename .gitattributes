# Принудительно использовать LF (Linux) переносы для скриптов
# Это решает проблему запуска в WSL/Linux (ошибка /usr/bin/env: python3\r)
*.py text eol=lf
*.sh text eol=lf

# Для остальных файлов - автоопределение
* text=auto
